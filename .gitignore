# Listing these manually rather than *.so so that the lib symlinks do
# get checked in
/lib/antlr4-runtime.so
/lib/icl-parser.so
/lib/icl-parser.so
/lib/ijtag.so

# Misc
.irb_history

# Editor cruft
*.swp
*.swo
*~

# Origen local files
/.bundle
/target/.default
/environment/.default
/release_note.txt
/pkg
/lbin
/.bin
/list/referenced.list
/tags
/.ref
/.ws
/.lsf
/.db
/log
/output
/web
/coverage
/.ref
/.yardoc
/.collection
/.bin
/.session
/tmp

# Cruft from other revision control systems
.SYNC
.svn
